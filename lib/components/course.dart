class Course {

  int startColor, endColor, headlineColor;
  String courseTitle, courseHeadline, courseImage;
  Course(this.startColor, this.endColor, this.courseHeadline, this.courseImage, this.courseTitle, this.headlineColor);

}