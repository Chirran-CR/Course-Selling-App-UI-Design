class CourseDetail{
  String courseName, courseDuration, courseAvailability, courseImage;
  int courseColor;
  CourseDetail(this.courseName, this.courseDuration, this.courseAvailability, this.courseImage, this.courseColor);
  
}